[core]
	editor = emacs -nw
[user]
	name = Alexander Kurbatov
	email = a.kurbatov@mts.ai
[pull]
	rebase = true
[push]
	default = simple
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
